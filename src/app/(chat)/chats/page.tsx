
import { Box } from "@mui/material";

export default async function Page() {
 return (
  <Box className="flex grow m-0 p-0">
  <Box className="flex flex-col-reverse h-full w-full max-h-[90vh] overflow-scroll">
    <Box className="flex-grow-[9.5] flex items-end justify-center px-0" >
           Click the + button to start a new chat!
    </Box>
    </Box>
    </Box>
  );
  }